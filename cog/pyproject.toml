[project]
name = "cog"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "cogapp>=3.4.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
